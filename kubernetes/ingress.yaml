apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: single-price-grid-component
  namespace: default
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
    - host: mmalabugin.ru
      http:
        paths:
          - path: /SinglePriceGridComponent
            pathType: Prefix
            backend:
              service:
                name: single-price-grid-component
                port:
                  number: 80
  tls:
    - hosts:
        - mmalabugin.ru
      secretName: main-page-tls
