apiVersion: apps/v1
kind: Deployment
metadata:
  name: single-price-grid-component-app
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: single-price-grid-component-app
  template:
    metadata:
      labels:
        app: single-price-grid-component-app
    spec:
      containers:
        - name: single-price-grid-component-app
          image: single-price-grid-component-app:latest
          ports:
            - containerPort: 80
          env:
            - name: NODE_ENV
              value: "production"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
